# 🔒 Plantilla de variables de entorno
# Copia este archivo como .env.local y completa los valores

# Modo de operación: 1 = mock local, 0 = API real
USE_MOCK=0

# URL del API - 🔒 IMPORTANTE: Usar HTTPS en producción
NEXT_PUBLIC_API_URL=http://localhost:10931/preventa/api/v1

# URL para CORS
URL_API=localhost:3000

# 🔒 Claves de encriptación (CAMBIAR EN PRODUCCIÓN)
# Generar claves seguras con:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXT_PUBLIC_CRYPTO_SECRET=your-secure-secret-key-here
NEXT_PUBLIC_CRYPTO_SALT=your-secure-salt-here

# 🔒 NOTAS DE SEGURIDAD:
# 1. NUNCA commitear el archivo .env.local
# 2. Usar claves diferentes para desarrollo, staging y producción
# 3. Rotar claves periódicamente
# 4. En producción, usar HTTPS exclusivamente
